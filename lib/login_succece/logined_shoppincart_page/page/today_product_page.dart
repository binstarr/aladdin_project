import 'package:flutter/material.dart';

class LoginedTodayProduct extends StatelessWidget {
  const LoginedTodayProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
