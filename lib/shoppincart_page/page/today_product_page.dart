import 'package:flutter/material.dart';

class TodayProduct extends StatelessWidget {
  const TodayProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
